// List decodeString(String encodedString) {
//   String decodedString = utf8.decode(base64.decode(encodedString));
//   List decodedList = Uri.decodeComponent(decodedString).split('|');
//   return decodedList;
// }
// 